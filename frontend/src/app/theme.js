"use client";

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#7b9aa5",
    },
    secondary: {
      main: "#45505f",
    },
    error: {
      main: "#ba4030",
    },
    warning: {
      main: "#ba4030",
    },
    background: {
      default: "#f0efee9",
    },
  },
});

export default theme;
